# Security Policy

## Reporting

Do not open a public issue for leaked keys, account access problems, or private media URLs.

Report security issues to the repository owner or EvoLink support with sanitized reproduction steps, affected package version, and expected impact.

## Secrets

Never commit EvoLink API keys, generated private output, local `.env` files, or user media URLs. Examples read `EVOLINK_API_KEY` from the environment.
